<UserControl x:Class="JunkCodeRemover.JunkCodeRemoverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
            >
    <Grid Height="550" Width="580">
        <Grid.RowDefinitions>
            <RowDefinition Height="22*"/>
            <RowDefinition Height="235*"/>
            <RowDefinition Height="18*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Junk Code Remover" Foreground="White" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBox Margin="0" AcceptsReturn="True" Text="{Binding HTML}" Grid.Row="1" />
        <Button Grid.Row="2" Margin="100, 2, 100, 2" Content="Clear Formatting" Command="{Binding SanitizeCommand}"/>
    </Grid>
</UserControl>
